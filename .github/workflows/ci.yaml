name: Continuous Integration

on:
  pull_request:
    branches:
      - main

jobs:
  build:
    name: Build
    uses: ./.github/workflows/build.yaml
    with:
        node-versions: >-
          ["20.x", "22.x", "24.x"]
    secrets: inherit
